---
phase: 05-seo-polish
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/components/SiteHeader.astro
autonomous: true
requirements:
  - SEO-01
  - SEO-02

must_haves:
  truths:
    - "Dark mode tokens override all CSS custom properties when data-theme='dark' is on <html>"
    - "SiteHeader renders site name (left), RSS link and toggle button (right) in a sticky bar"
    - "Toggle button shows moon icon in light mode and sun icon in dark mode"
    - "Clicking the toggle flips data-theme on <html> and persists to localStorage('theme')"
    - "Toggle aria-label updates dynamically to reflect current mode"
  artifacts:
    - path: "src/styles/global.css"
      provides: "[data-theme='dark'] CSS block with all 10 token overrides"
      contains: "[data-theme=\"dark\"]"
    - path: "src/components/SiteHeader.astro"
      provides: "Sticky header with site name, RSS link, and dark mode toggle"
      exports: ["default (Astro component)"]
  key_links:
    - from: "src/components/SiteHeader.astro toggle script"
      to: "document.documentElement"
      via: "setAttribute('data-theme', ...)"
      pattern: "setAttribute.*data-theme"
    - from: "src/components/SiteHeader.astro toggle script"
      to: "localStorage"
      via: "localStorage.setItem('theme', ...)"
      pattern: "localStorage\\.setItem"
    - from: "src/styles/global.css"
      to: "all component style rules"
      via: "[data-theme='dark'] CSS custom property overrides"
      pattern: "\\[data-theme=\"dark\"\\]"
---

<objective>
Add the dark mode CSS token block to `global.css` and create the `SiteHeader.astro` component with the RSS link and dark mode toggle button.

Purpose: Deliver the dark mode feature and persistent site header — two visible Phase 5 deliverables. These are created independently of Plan 01 (RSS/sitemap) and can run in parallel.
Output: `global.css` with `[data-theme="dark"]` overrides; `SiteHeader.astro` with sticky layout, RSS link, and toggle button with click script.
</objective>

<execution_context>
@/Users/jmcneilly/ghorg/jcbmcn/jcbmcn.github.io/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/jmcneilly/ghorg/jcbmcn/jcbmcn.github.io/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-seo-polish/05-CONTEXT.md
@.planning/phases/05-seo-polish/05-UI-SPEC.md
@.planning/phases/05-seo-polish/05-RESEARCH.md

<interfaces>
<!-- Key contracts extracted from codebase -->

From src/styles/global.css (current state — the stub comment to replace):
```css
/* Dark mode token overrides — values stubbed for v2 */
/* [data-theme="dark"] { ... } */
```
Replace this 2-line comment with the real [data-theme="dark"] block.

Light mode tokens already in :root (DO NOT change these):
- --color-bg, --color-surface, --color-surface-alt, --color-border
- --color-text-primary, --color-text-secondary, --color-text-muted
- --color-accent: #6366F1, --color-accent-hover, --color-accent-subtle

Dark mode values to use (from D-14 / UI-SPEC Color section):
- --color-bg: #0F172A
- --color-surface: #1E293B
- --color-surface-alt: #334155
- --color-border: #334155
- --color-text-primary: #F8FAFC
- --color-text-secondary: #94A3B8
- --color-text-muted: #64748B
- --color-accent: #818CF8
- --color-accent-hover: #A5B4FC
- --color-accent-subtle: rgba(99, 102, 241, 0.15)

SiteHeader layout spec (from UI-SPEC Component Specs):
- height: 56px, padding: 0 1.5rem
- background: var(--color-surface), border-bottom: 1px solid var(--color-border)
- position: sticky; top: 0; z-index: 10
- flex layout: space-between, align-items center
- Left: <a href="/">Jacob McNeilly</a> — 16px/700/var(--color-text-primary)
- Right (flex row, gap 0.5rem): RSS link then toggle button
- RSS link: <a href="/rss.xml" aria-label="RSS feed"> — text "RSS", 14px/400/var(--color-accent)
- Toggle: <button type="button" id="theme-toggle"> — 36×36px, border-radius 0.5rem

Sun SVG (shown in dark mode — click to go light):
  <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <circle cx="12" cy="12" r="5"/>
    <line x1="12" y1="1" x2="12" y2="3"/>
    <line x1="12" y1="21" x2="12" y2="23"/>
    <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/>
    <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/>
    <line x1="1" y1="12" x2="3" y2="12"/>
    <line x1="21" y1="12" x2="23" y2="12"/>
    <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/>
    <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
  </svg>

Moon SVG (shown in light mode — click to go dark):
  <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>
  </svg>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add dark mode CSS token block to global.css</name>
  <files>src/styles/global.css</files>
  <read_first>
    - src/styles/global.css — read the full file; locate the 2-line stub comment to replace
  </read_first>
  <action>
In `src/styles/global.css`, find these two lines inside the `@layer base` block:
```css
  /* Dark mode token overrides — values stubbed for v2 */
  /* [data-theme="dark"] { ... } */
```

Replace them with the real dark mode token block (per D-14 and UI-SPEC Color section):
```css
  [data-theme="dark"] {
    --color-bg: #0F172A;
    --color-surface: #1E293B;
    --color-surface-alt: #334155;
    --color-border: #334155;
    --color-text-primary: #F8FAFC;
    --color-text-secondary: #94A3B8;
    --color-text-muted: #64748B;
    --color-accent: #818CF8;
    --color-accent-hover: #A5B4FC;
    --color-accent-subtle: rgba(99, 102, 241, 0.15);
  }
```

Do NOT modify any other part of `global.css`. The `:root` light mode tokens, `html`, `body`, and `a` rules must remain untouched.

All components already use `var(--color-*)` custom properties — dark mode is automatic once these overrides are in place. No component-level changes needed.
  </action>
  <verify>
    <automated>grep -c "\[data-theme=\"dark\"\]" src/styles/global.css</automated>
  </verify>
  <acceptance_criteria>
    - `src/styles/global.css` contains `[data-theme="dark"]` block (grep returns 1)
    - Block contains `--color-bg: #0F172A` (grep: `grep -c "#0F172A" src/styles/global.css`)
    - Block contains `--color-accent: #818CF8` (grep: `grep -c "#818CF8" src/styles/global.css`)
    - Block contains all 10 custom property overrides (count: `grep -c "\-\-color-" src/styles/global.css` increases by 10 from original 10 to 20)
    - Original stub comment `/* [data-theme="dark"] { ... } */` is gone (grep returns 0)
    - `npx astro build` still exits 0 after CSS change
  </acceptance_criteria>
  <done>global.css has working [data-theme="dark"] block with all 10 token overrides; build passes</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create SiteHeader.astro component</name>
  <files>src/components/SiteHeader.astro</files>
  <read_first>
    - src/components/PostCard.astro — reference for how Astro component scoped style blocks are structured in this project
    - src/styles/global.css — confirm token names (--color-surface, --color-border, --color-accent, --color-accent-hover, --color-surface-alt, --color-text-primary)
    - .planning/phases/05-seo-polish/05-UI-SPEC.md — exact layout spec, copy, and SVG icons
  </read_first>
  <action>
Create `src/components/SiteHeader.astro` with this structure:

**Frontmatter:** empty (no props needed — content is static)

**HTML template:**
```html
<header class="site-header">
  <a href="/" class="site-name">Jacob McNeilly</a>
  <div class="header-controls">
    <a href="/rss.xml" class="rss-link" aria-label="RSS feed">RSS</a>
    <button type="button" id="theme-toggle" aria-label="Switch to dark mode">
      <span class="icon-moon">
        <!-- Moon SVG — visible in light mode -->
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>
        </svg>
      </span>
      <span class="icon-sun" style="display:none">
        <!-- Sun SVG — visible in dark mode -->
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="12" cy="12" r="5"/>
          <line x1="12" y1="1" x2="12" y2="3"/>
          <line x1="12" y1="21" x2="12" y2="23"/>
          <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/>
          <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/>
          <line x1="1" y1="12" x2="3" y2="12"/>
          <line x1="21" y1="12" x2="23" y2="12"/>
          <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/>
          <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
        </svg>
      </span>
    </button>
  </div>
</header>
```

**Toggle script** (use `<script>` — bundled is fine, runs after paint):
```javascript
const btn = document.getElementById('theme-toggle');
const moonIcon = btn?.querySelector('.icon-moon') as HTMLElement | null;
const sunIcon = btn?.querySelector('.icon-sun') as HTMLElement | null;

function applyTheme(theme: 'light' | 'dark') {
  document.documentElement.setAttribute('data-theme', theme);
  if (moonIcon && sunIcon) {
    moonIcon.style.display = theme === 'light' ? '' : 'none';
    sunIcon.style.display = theme === 'dark' ? '' : 'none';
  }
  if (btn) {
    btn.setAttribute('aria-label',
      theme === 'dark' ? 'Switch to light mode' : 'Switch to dark mode'
    );
  }
}

// Sync icon state with current theme on load
const currentTheme = document.documentElement.getAttribute('data-theme') as 'light' | 'dark' ?? 'light';
applyTheme(currentTheme);

btn?.addEventListener('click', () => {
  const next = document.documentElement.getAttribute('data-theme') === 'dark' ? 'light' : 'dark';
  localStorage.setItem('theme', next);
  applyTheme(next);
});
```

**Scoped styles** (use `<style>` block — Astro scopes automatically):
```css
.site-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 56px;
  padding: 0 1.5rem;
  background-color: var(--color-surface);
  border-bottom: 1px solid var(--color-border);
  position: sticky;
  top: 0;
  z-index: 10;
}

.site-name {
  font-size: 1rem;
  font-weight: 700;
  color: var(--color-text-primary);
  text-decoration: none;
}

.site-name:hover {
  color: var(--color-accent);
}

.header-controls {
  display: flex;
  align-items: center;
  gap: 0.5rem;
}

.rss-link {
  font-size: 0.875rem;
  font-weight: 400;
  color: var(--color-accent);
  text-decoration: none;
  padding: 0.25rem 0.5rem;
}

.rss-link:hover {
  color: var(--color-accent-hover);
}

#theme-toggle {
  width: 36px;
  height: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: transparent;
  border: none;
  border-radius: 0.5rem;
  cursor: pointer;
  color: var(--color-text-primary);
  padding: 0.5rem;
}

#theme-toggle:hover {
  background-color: var(--color-surface-alt);
}

#theme-toggle:focus-visible {
  outline: 2px solid var(--color-accent);
  outline-offset: 2px;
}
```

Note on icon initial state: The HTML sets moon visible / sun hidden by default (matching light mode). The toggle script's `applyTheme(currentTheme)` call on load corrects the icon immediately if the anti-flash script already set dark mode before the component rendered.
  </action>
  <verify>
    <automated>test -f src/components/SiteHeader.astro &amp;&amp; echo "PASS: file exists" &amp;&amp; grep -c "theme-toggle" src/components/SiteHeader.astro &amp;&amp; npx astro build 2>&amp;1 | grep -iE "error" | grep -v "sitemap\|rss" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/SiteHeader.astro` exists
    - Contains `id="theme-toggle"` (grep returns 1+)
    - Contains `aria-label="RSS feed"` on the RSS link
    - Contains `href="/rss.xml"` for the RSS link
    - Contains `href="/"` for the site name link with text "Jacob McNeilly"
    - Contains `localStorage.setItem('theme'` in the script block
    - Contains `setAttribute('data-theme'` in the script block
    - Contains `.site-header` CSS with `position: sticky`
    - Contains `[data-theme` CSS selector OR relies on global.css tokens (var(--color-*))
    - `npx astro build` exits 0 (component compiles without TypeScript errors)
  </acceptance_criteria>
  <done>SiteHeader.astro created with sticky layout, RSS link, moon/sun toggle, and click script that persists to localStorage</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `src/styles/global.css` contains `[data-theme="dark"]` block with all 10 overrides
2. `src/components/SiteHeader.astro` exists with RSS link, toggle button, and toggle script
3. `npx astro build` exits 0
4. Manual check: Open site in browser, toggle should flip data-theme on `<html>` (check DevTools)
</verification>

<success_criteria>
- Dark mode CSS tokens wired — all components darken automatically when data-theme="dark" is set
- SiteHeader component created, ready to be imported in BaseLayout.astro (Plan 03)
- No new TypeScript or build errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/05-seo-polish/05-02-SUMMARY.md`
</output>
