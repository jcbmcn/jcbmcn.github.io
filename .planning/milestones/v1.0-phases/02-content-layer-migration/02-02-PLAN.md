---
phase: 02-content-layer-migration
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/content/blog/2025-02-13-power-automate.md
  - src/content/blog/2025-08-19-talosctl-cdktf-guide.md
  - src/content/blog/2025-12-28-building-codeowners-simulator.md
autonomous: true
requirements:
  - CONT-02
  - CONT-03

must_haves:
  truths:
    - "All 3 real posts exist in src/content/blog/ with content identical to the pre-deletion originals"
    - "npm run build exits 0 with all 3 posts present — schema from Plan 01 validates all frontmatter"
    - "Each post URL slug matches Hugo's :filename pattern — no date prefix stripped"
    - "test-draft.md is still present and still excluded from the build output (draft filtering intact)"
  artifacts:
    - path: "src/content/blog/2025-02-13-power-automate.md"
      provides: "Power Automate post — has both images[] and featuredImage, plus fontawesome"
    - path: "src/content/blog/2025-08-19-talosctl-cdktf-guide.md"
      provides: "CDKTF/Talos post — has code.copy nested object"
    - path: "src/content/blog/2025-12-28-building-codeowners-simulator.md"
      provides: "CODEOWNERS simulator post — has code.copy nested object"
  key_links:
    - from: "src/content/blog/*.md"
      to: "src/content.config.ts"
      via: "glob loader — all .md files in src/content/blog/ are validated against the schema at build time"
      pattern: "glob.*src/content/blog"
---

<objective>
Restore all 3 real blog posts from git history into `src/content/blog/` and verify the full collection builds cleanly.

Purpose: The posts were deleted from `content/posts/` in the Phase 1 Hugo removal commit (`44bc5d5`). This plan recovers them verbatim using `git show` on the pre-deletion commit, places them at the correct path under the new content collection, and confirms `npm run build` succeeds with all posts present. No frontmatter edits — post content is copied exactly as-is (D-06).

Output: 3 migrated post files in `src/content/blog/`, all validating against the schema created in Plan 01, with URL slugs matching Hugo's `:filename` pattern.
</objective>

<execution_context>
@/Users/jmcneilly/ghorg/jcbmcn/jcbmcn.github.io/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/jmcneilly/ghorg/jcbmcn/jcbmcn.github.io/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-content-layer-migration/02-CONTEXT.md
@.planning/phases/02-content-layer-migration/02-01-SUMMARY.md

<interfaces>
<!-- Source posts live at git commit 44bc5d5^ (parent of Hugo removal commit 44bc5d5) -->
<!-- Recover with: git show 44bc5d5^:content/posts/<filename> -->

<!-- Post 1: content/posts/2025-02-13-power-automate.md -->
<!-- Frontmatter: title, subtitle, author, date: '2025-02-13', draft: false, tags: [...], images: ['/images/blog/robot-arm.jpg'], featuredImage: '/images/blog/robot-arm.jpg', fontawesome: true -->

<!-- Post 2: content/posts/2025-08-19-talosctl-cdktf-guide.md -->
<!-- Frontmatter: date: '2025-08-28', title, author, draft: false, tags: [...], featuredImage: '/images/blog/cdktf_py_banner.png', code: { copy: true } -->
<!-- NOTE: The file is named 2025-08-19 but the date field says '2025-08-28' — copy verbatim, do not "fix" -->

<!-- Post 3: content/posts/2025-12-28-building-codeowners-simulator.md -->
<!-- Frontmatter: title, author, date: '2025-12-28', draft: false, tags: [...], featuredImage: '/images/blog/codeowners-simulator.png', code: { copy: true } -->

<!-- Target location for all 3: src/content/blog/ (same filename, no changes) -->
<!-- URL slug: Astro's glob loader uses the base filename as the entry id -->
<!-- e.g., 2025-12-28-building-codeowners-simulator.md → slug: 2025-12-28-building-codeowners-simulator -->
<!-- Matches Hugo :filename permalink: /2025-12-28-building-codeowners-simulator/ ✓ -->

<!-- Images already in public/images/blog/ from Phase 1 Plan 02 — no image files to move -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore 3 posts from git history</name>
  <files>
    src/content/blog/2025-02-13-power-automate.md,
    src/content/blog/2025-08-19-talosctl-cdktf-guide.md,
    src/content/blog/2025-12-28-building-codeowners-simulator.md
  </files>
  <read_first>
    - src/content.config.ts — verify the full schema from Plan 01 is in place before writing posts
    - src/content/blog/ — confirm directory exists and contains only test-draft.md
  </read_first>
  <action>
    Restore all 3 posts verbatim from the pre-deletion commit. Run these exact commands:

    ```bash
    git show 44bc5d5^:content/posts/2025-02-13-power-automate.md > src/content/blog/2025-02-13-power-automate.md
    git show 44bc5d5^:content/posts/2025-08-19-talosctl-cdktf-guide.md > src/content/blog/2025-08-19-talosctl-cdktf-guide.md
    git show 44bc5d5^:content/posts/2025-12-28-building-codeowners-simulator.md > src/content/blog/2025-12-28-building-codeowners-simulator.md
    ```

    Do NOT edit any frontmatter or body content after copying — per D-06, posts are copied exactly as-is.
    Do NOT delete or modify test-draft.md.

    Then run `npm run build` to verify all 3 posts validate against the schema without errors.

    If `npm run build` fails with a schema validation error, read the error message carefully:
    - If a field is not recognized → the schema in Plan 01 may need the field added (check src/content.config.ts)
    - If a field type mismatch → check whether the field needs z.coerce (e.g., date)
    - Do NOT edit post frontmatter to work around schema issues — fix the schema instead

    After a clean build, check that the 3 posts appear in the build output at the correct paths:
    ```bash
    ls dist/ 2>/dev/null || echo "Build outputs to dist/"
    # Verify via build log that pages were generated for the 3 posts
    ```
  </action>
  <verify>
    <automated>npm run build && echo "BUILD_OK" && test -f src/content/blog/2025-02-13-power-automate.md && test -f src/content/blog/2025-08-19-talosctl-cdktf-guide.md && test -f src/content/blog/2025-12-28-building-codeowners-simulator.md && echo "ALL_POSTS_PRESENT"</automated>
  </verify>
  <done>
    - All 3 files exist in src/content/blog/ with content matching the pre-deletion originals
    - npm run build exits 0 with all 3 posts plus test-draft.md present
    - No schema validation errors in build output
    - test-draft.md still present (not accidentally deleted)
    - Post content is verbatim — no frontmatter edits made
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify URL slugs and image field edge cases</name>
  <files></files>
  <read_first>
    - src/content/blog/2025-02-13-power-automate.md — verify both images[] and featuredImage fields are present (CONT-04)
    - src/content/blog/2025-08-19-talosctl-cdktf-guide.md — verify code.copy field present
    - src/content/blog/2025-12-28-building-codeowners-simulator.md — verify code.copy field present
  </read_first>
  <action>
    This task verifies all CONT-03 and CONT-04 acceptance criteria are met. No files are modified.

    **CONT-03 — URL slug verification:**
    Astro's glob loader uses the base filename (without extension) as the entry `id`. Verify that each post's collection entry id matches the Hugo permalink pattern:
    - `2025-02-13-power-automate`
    - `2025-08-19-talosctl-cdktf-guide`
    - `2025-12-28-building-codeowners-simulator`

    Run a quick check via an inline Astro script or verify by inspecting the build output:
    ```bash
    # Check build output for post routes
    find dist -name "index.html" | grep -E "2025-" | sort
    ```
    Expected: each post slug appears as a directory in dist/ (Astro creates dist/{slug}/index.html for static pages if templates exist, but since Phase 3 hasn't been built yet, this may only show the collection entry id in build logs).

    Actually, since there are no page templates for blog posts yet (Phase 3 adds those), the posts exist in the content collection but won't produce HTML routes until Phase 3. The slug is the collection entry id, which is the filename. Verify the schema validation passes (the build succeeds) as the primary CONT-03 check — the slug-to-URL mapping will be verified by Phase 3.

    **CONT-04 — Image field edge cases:**
    Grep the migrated posts to confirm the edge case fields are preserved verbatim:
    ```bash
    grep -n "images\|featuredImage" src/content/blog/2025-02-13-power-automate.md
    # Expected: both fields present
    grep -n "code:" src/content/blog/2025-08-19-talosctl-cdktf-guide.md
    grep -n "copy:" src/content/blog/2025-08-19-talosctl-cdktf-guide.md
    # Expected: code: block with copy: true
    ```

    No fixes needed — this task is verification only. If fields are missing, the git show commands in Task 1 did not copy verbatim; re-run them.
  </action>
  <verify>
    <automated>grep -q "featuredImage" src/content/blog/2025-02-13-power-automate.md && grep -q "images:" src/content/blog/2025-02-13-power-automate.md && grep -q "copy: true" src/content/blog/2025-08-19-talosctl-cdktf-guide.md && grep -q "copy: true" src/content/blog/2025-12-28-building-codeowners-simulator.md && echo "EDGE_CASES_OK"</automated>
  </verify>
  <done>
    - power-automate.md has both images[] and featuredImage fields (CONT-04 dual-field edge case)
    - Both talosctl and codeowners posts have code.copy nested object preserved verbatim
    - npm run build still exits 0 (no regression from verification step)
    - CONT-02, CONT-03, CONT-04 acceptance criteria all checkable via grep/build
  </done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 with all 4 posts (3 real + test-draft) in src/content/blog/
- `test -f src/content/blog/2025-02-13-power-automate.md` — passes
- `test -f src/content/blog/2025-08-19-talosctl-cdktf-guide.md` — passes
- `test -f src/content/blog/2025-12-28-building-codeowners-simulator.md` — passes
- `grep -q "images:" src/content/blog/2025-02-13-power-automate.md` — passes (dual image fields)
- `grep -q "copy: true" src/content/blog/2025-08-19-talosctl-cdktf-guide.md` — passes (nested code.copy)
- test-draft.md still present (draft filtering not broken)
</verification>

<success_criteria>
All 3 real posts present in src/content/blog/, content identical to pre-deletion originals, npm run build exits 0. All frontmatter edge cases (dual image fields, nested code.copy, fontawesome) validated by the schema without errors. CONT-01 through CONT-04 all satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-layer-migration/02-02-SUMMARY.md`
</output>
